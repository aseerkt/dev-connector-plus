mutation UpdateProfile($profileInput: ProfileInputType!) {
  updateProfile(profileInput: $profileInput) {
    profile {
      ...RegularProfile
    }
    errors {
      path
      message
    }
  }
}
